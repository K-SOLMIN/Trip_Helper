# Backend origin for production.
# Leave empty in local development so Vite proxies /api to http://localhost:3001.
# Do not include the trailing /api path here.
VITE_API_BASE=https://your-backend.example.com
